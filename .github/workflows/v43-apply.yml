name: apply-v43-global

on:
  workflow_dispatch:
  push:
    branches: [feature/v4.3-global-intelligence]
    paths:
      - scripts/apply-v43-global.js
      - .github/workflows/v43-apply.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/v4.3-global-intelligence
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Apply v4.3 migration
        run: node scripts/apply-v43-global.js
      - name: Run unit tests
        run: npm test
      - name: Check JavaScript syntax
        run: npm run syntax
      - name: Commit generated v4.3 integration
        run: |
          if git diff --quiet; then
            echo "No generated changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add R4G3RUNN3R-Recruitment-Agency.user.js
          git commit -m "feat: integrate v4.3 global intelligence client"
          git push origin HEAD:feature/v4.3-global-intelligence
