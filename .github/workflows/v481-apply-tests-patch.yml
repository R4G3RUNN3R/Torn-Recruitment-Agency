name: Apply v4.8.1 regression patch

on:
  push:
    branches:
      - fix/v481-active-search
    paths:
      - tmp/v481-tests.patch
      - .github/workflows/v481-apply-tests-patch.yml

permissions:
  contents: write

jobs:
  apply-tests-patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: fix/v481-active-search
          fetch-depth: 0

      - name: Apply verified regression patch
        run: |
          git apply --check tmp/v481-tests.patch
          git apply tmp/v481-tests.patch
          rm tmp/v481-tests.patch
          rm .github/workflows/v481-apply-tests-patch.yml

      - name: Commit exact regression tests
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "test: cover active forum and API search"
          git push origin HEAD:fix/v481-active-search
