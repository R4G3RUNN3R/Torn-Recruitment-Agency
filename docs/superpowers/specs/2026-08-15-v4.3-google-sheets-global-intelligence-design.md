# Recruitment Agency v4.3 Google Sheets Global Intelligence Design

## Goal

Add an optional shared intelligence layer to Recruitment Agency using a private Google Sheet as the global data store and a Google Apps Script web app as the public read/write API.

Any Recruitment Agency user who legitimately obtains Torn player data through the script can contribute a sanitized observation. Other users can then retrieve that player's previous observations and compare current activity against historical shared