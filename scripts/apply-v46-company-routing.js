const fs=require('node:fs');
const path=require('node:path');
const p=path.join(__dirname,'..','src','v45-app.js');
let text=fs.readFileSync(p,'utf8');
function once(label,oldText,newText){const n=text.split(oldText).length-1;if(n!==1)throw new Error(`${label}: expected 1, found ${n}`);text=text.replace(oldText,newText);}
function all(label,oldText,newText){const n=text.split(oldText).length-1;if(n<1)throw new Error(`${label}: expected >=1, found ${n}`);text=text.split(oldText).join(newText);console.log(`${label}: ${n}`);}

once('default route and group',"launcherEnabled:true, includeInactive:false, activePage:'overview', navigation:{expandedGroups:['recruitment']}, apiKey:'', ownCompanyName:'',","launcherEnabled:true, includeInactive:false, activePage:'company-overview', navigation:{expandedGroups:['company-recruitment']}, apiKey:'', ownCompanyName:'',");
once('initial state page',"db:null, settings:null, page:'overview', mounted:false","db:null, settings:null, page:'company-overview', mounted:false");
once('company icons',"function icon(id){return({overview:'⌂',discover:'⌕',candidates:'◎',pipeline:'▦',scout:'◈','smart-match':'◇','global-intelligence':'◉',settings:'⚙',data:'▤',logs:'≡'})[id]||'•';}","function icon(id){return({'company-overview':'⌂','company-today':'✓','company-discover':'⌕','company-candidates':'◎','company-pipeline':'▦','company-vacancies':'▥',scout:'◈','smart-match':'◇','global-intelligence':'◉',settings:'⚙',data:'▤',logs:'≡'})[id]||'•';}");
once('scout refresh routes',"if(state.page==='scout'||state.page==='candidates'||state.page==='pipeline')await route(state.page,false);","if(state.page==='scout'||state.page==='company-candidates'||state.page==='company-pipeline')await route(state.page,false);");
once('company discovery helpers',
"  function recruitmentDomainForFeed(feed={}){const feedId=text(feed.feedId).toLowerCase();const sourceType=text(feed.sourceType).toUpperCase();return feedId==='faction'||sourceType==='FACTION FORUM'?'faction':'company';}\n",
"  function recruitmentDomainForFeed(feed={}){const feedId=text(feed.feedId).toLowerCase();const sourceType=text(feed.sourceType).toUpperCase();return feedId==='faction'||sourceType==='FACTION FORUM'?'faction':'company';}\n  function companyDiscoveryFeeds(settings=state.settings?.recruitment||{}){return Discovery.feedDefinitions(settings).filter(feed=>recruitmentDomainForFeed(feed)==='company');}\n  async function resetCompanyForumImport(){for(const source of await idb.getAll('forumSources'))if(recruitmentDomainForFeed({sourceType:source?.sourceType})==='company')await idb.delete('forumSources',source.sourceId);for(const checkpoint of await idb.getAll('forumSyncState'))if(['company','training'].includes(text(checkpoint?.feedId).toLowerCase()))await idb.delete('forumSyncState',checkpoint.feedId);return true;}\n");
once('company-only sync feeds',"for(const feed of Discovery.feedDefinitions(state.settings.recruitment).filter(f=>f.enabled))","for(const feed of companyDiscoveryFeeds(state.settings.recruitment).filter(f=>f.enabled))");
once('sync refresh routes',"if(state.page==='discover'||state.page==='candidates'||state.page==='overview')await route(state.page,false);","if(state.page==='company-discover'||state.page==='company-candidates'||state.page==='company-overview')await route(state.page,false);");
once('fill refresh routes',"if(state.page==='discover'||state.page==='candidates')await route(state.page,false);","if(state.page==='company-discover'||state.page==='company-candidates')await route(state.page,false);");
once('page metadata',
"  function pageMeta(page){return({overview:['Overview','Recruitment status and work needing attention.'],discover:['Discover','Sync official Torn forum recruitment sources and enrich candidates.'],candidates:['Candidates','Search, filter and manage the unified local candidate workspace.'],pipeline:['Pipeline','Move candidates through the exact six approved stages.'],scout:['Scout','Collect official Torn player intelligence through the shared scheduler.'],'smart-match':['Smart Match','Build local vacancy profiles and explain candidate Match scores.'],'global-intelligence':['Global Intelligence','Review the sanitized shared intelligence service.'],settings:['Settings','Application, recruitment, Scout, candidate and privacy configuration.'],data:['Data','Local IndexedDB counts, export and reset controls.'],logs:['Logs','Sanitized Recruitment Agency diagnostic events.']})[page]||['Overview','Recruitment Agency'];}",
"  function pageMeta(page){return({'company-overview':['Company Overview','Company recruitment status and work needing attention.'],'company-today':['Company Today','Prioritized Company recruitment actions from local workflow data.'],'company-discover':['Company Discover','Sync Company and Train Buyer Torn forum sources only.'],'company-candidates':['Company Candidates','Search, filter and manage Company recruitment candidates.'],'company-pipeline':['Company Pipeline','Move Company candidates through the approved recruitment stages.'],'company-vacancies':['Company Vacancies','Manage Company baseline requirements and versioned vacancies.'],scout:['Scout','Collect official Torn player intelligence through the shared scheduler.'],'smart-match':['Smart Match','Build local vacancy profiles and explain candidate Match scores.'],'global-intelligence':['Global Intelligence','Review the sanitized shared intelligence service.'],settings:['Settings','Application, recruitment, Scout, candidate and privacy configuration.'],data:['Data','Local IndexedDB counts, export and reset controls.'],logs:['Logs','Sanitized Recruitment Agency diagnostic events.']})[page]||['Company Overview','Recruitment Agency'];}");
once('overview source scope',"async function renderOverview(){const views=await candidateViews();const sources=await idb.getAll('forumSources');","async function renderOverview(){const views=await candidateViews();const sources=(await idb.getAll('forumSources')).filter(source=>recruitmentDomainForFeed({sourceType:source?.sourceType})==='company');");
once('discover source scope',"async function renderDiscover(){const feeds=Discovery.feedDefinitions(state.settings.recruitment);const checkpoints=new Map((await idb.getAll('forumSyncState')).map(x=>[x.feedId,x]));const sources=(await idb.getAll('forumSources')).sort((a,b)=>Number(b.postedAt||0)-Number(a.postedAt||0)).slice(0,12);","async function renderDiscover(){const feeds=companyDiscoveryFeeds(state.settings.recruitment);const checkpoints=new Map((await idb.getAll('forumSyncState')).map(x=>[x.feedId,x]));const sources=(await idb.getAll('forumSources')).filter(source=>recruitmentDomainForFeed({sourceType:source?.sourceType})==='company').sort((a,b)=>Number(b.postedAt||0)-Number(a.postedAt||0)).slice(0,12);");
once('remove Faction thread button',"<button class=\"ra-btn\" data-open-thread=\"faction\">Open Faction Thread</button>","");
once('company reset binding',"document.getElementById('ra-reset-forum')?.addEventListener('click',async()=>{if(!confirm('Reset local forum import sources and checkpoints? Candidate recruiter data is preserved.'))return;await idb.clear('forumSources');await idb.clear('forumSyncState');toast('Forum import reset.');await route('discover',false);});","document.getElementById('ra-reset-forum')?.addEventListener('click',async()=>{if(!confirm('Reset local Company/Training forum import sources and checkpoints? Faction history and recruiter data are preserved.'))return;await resetCompanyForumImport();toast('Company forum import reset.');await route('company-discover',false);});");
once('renderers and temporary Company pages',
"  const renderers={overview:renderOverview,discover:renderDiscover,candidates:renderCandidates,pipeline:renderPipeline,scout:renderScout,'smart-match':renderSmartMatch,'global-intelligence':renderGlobal,settings:renderSettings,data:renderData,logs:renderLogs};",
"  async function renderCompanyToday(){return panel('Company Today','Prioritized Company recruitment actions','<div class=\"ra-muted\">Company action prioritization is being completed in this Company Core slice.</div>');}\n  async function renderCompanyVacancies(){return panel('Company Vacancies','Baseline and vacancy configuration','<div class=\"ra-muted\">Company baseline and vacancy controls are being completed in this Company Core slice.</div>');}\n  const renderers={'company-overview':renderOverview,'company-today':renderCompanyToday,'company-discover':renderDiscover,'company-candidates':renderCandidates,'company-pipeline':renderPipeline,'company-vacancies':renderCompanyVacancies,scout:renderScout,'smart-match':renderSmartMatch,'global-intelligence':renderGlobal,settings:renderSettings,data:renderData,logs:renderLogs};");
all('candidate route refreshes',"route('candidates',false)","route('company-candidates',false)");
all('pipeline route refreshes',"route('pipeline',false)","route('company-pipeline',false)");
all('discover route refreshes',"route('discover',false)","route('company-discover',false)");
all('overview route refreshes',"route('overview',false)","route('company-overview',false)");
once('hard reset state page',"state.page='overview';","state.page='company-overview';");
once('test exports',"_test:{state,repositories,recruitmentDomainForFeed,persistDiscoveredCandidate,companyCandidateViews,saveCompanyRecruitmentPatch,deleteCompanyCandidateData,clearRecruitmentData,applyCandidateFilters,candidateCsvRow,matchAvailability,forumThreadUrl}});","_test:{state,repositories,recruitmentDomainForFeed,companyDiscoveryFeeds,resetCompanyForumImport,persistDiscoveredCandidate,companyCandidateViews,saveCompanyRecruitmentPatch,deleteCompanyCandidateData,clearRecruitmentData,applyCandidateFilters,candidateCsvRow,matchAvailability,forumThreadUrl}});");
fs.writeFileSync(p,text);console.log('Applied guarded Company routing patch.');
